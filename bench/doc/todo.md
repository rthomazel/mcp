# && auto split feature mangles heredocs

input:

["cat >> internal/communications/tasks_test.go << 'GOEOF'\n\nfunc TestService_MemberTaskNotification_text(t *testing.T) {\n\tassert := assert.New(t)\n\n\tmember := &domain.Member{\n\t\tID: entityid.DefaultGenerator.Generate(),\n\t\tNameFirst: \"John\",\n\t\tPhoneMobile: \"123-123-1234\",\n\t\tConsentToText: true,\n\t}\n\n\tctx := context.Background()\n\tmemberAppTasksURL := \"eleanor.health.com/tasks\"\n\n\temailSvc := email.NewMockService(t)\n\tsmsSvc := sms.NewMockService(t)\n\tmemberServerClient := memberserver.NewMockClient(t)\n\tthrottler := throttle.NewMockThrottler(t)\n\n\tdateEmailVerified := time.Now()\n\tmemberServerClient.On(\"Members\", ctx, &mserver.MembersOptions{\n\t\tHubID: member.ID,\n\t}).Return([]*mserver.Member{\n\t\t{DateEmailVerified: &dateEmailVerified},\n\t}, nil).Once()\n\n\tmetadata := map[string]string{\n\t\tcomms.MetadataKeyHubMemberID: member.ID,\n\t}\n\n\tsmsSvc.On(\"SendMessage\", ctx, member.PhoneMobile, mock.MatchedBy(func(body string) bool {\n\t\treturn strings.Contains(body, memberAppTasksURL) && strings.Contains(body, member.NameFirst)\n\t}), metadata).Return(nil).Once()\n\n\tff := featureflag.NewMockService(t)\n\tff.On(\"Bool\", ctx, \"ff-se-2887\", false).Return(false, nil).Once()\n\n\tcommunicationsSvc := NewService(emailSvc, smsSvc, memberServerClient, throttler, \"noreply@eleanorhealth.com\", ff)\n\n\terr := communicationsSvc.MemberTaskNotification(ctx, member, memberAppTasksURL)\n\tassert.NoError(err)\n}\n\nfunc TestService_MemberTaskNotification_smsDisabledByFlag_fallsBackToEmail(t *testing.T) {\n\tassert := assert.New(t)\n\n\tmember := &domain.Member{\n\t\tID: entityid.DefaultGenerator.Generate(),\n\t\tNameFirst: \"John\",\n\t\tNameLast: \"Smith\",\n\t\tEmail: \"john.smith@eleanorhealth.com\",\n\t\tPhoneMobile: \"123-123-1234\",\n\t\tConsentToText: true,\n\t}\n\n\tctx := context.Background()\n\tmemberAppTasksURL := \"eleanor.health.com/tasks\"\n\n\temailSvc := email.NewMockService(t)\n\tsmsSvc := sms.NewMockService(t)\n\tmemberServerClient := memberserver.NewMockClient(t)\n\tthrottler := throttle.NewMockThrottler(t)\n\n\tdateEmailVerified := time.Now()\n\tmemberServerClient.On(\"Members\", ctx, &mserver.MembersOptions{\n\t\tHubID: member.ID,\n\t}).Return([]*mserver.Member{\n\t\t{DateEmailVerified: &dateEmailVerified},\n\t}, nil).Once()\n\n\tmetadata := map[string]string{\n\t\tcomms.MetadataKeyHubMemberID: member.ID,\n\t}\n\n\t// SMS is possible (member has phone + consent) but the flag forces email instead.\n\temailSvc.On(\"SendMessage\", ctx, \"Eleanor Health\", \"noreply@eleanorhealth.com\", member.FullName(), member.Email, \"New action item(s) from Eleanor Health\", \"This email must be viewed as HTML.\", mock.MatchedBy(func(body string) bool {\n\t\treturn strings.Contains(body, memberAppTasksURL) && strings.Contains(body, member.NameFirst)\n\t}), metadata).Return(nil).Once()\n\n\tff := featureflag.NewMockService(t)\n\tff.On(\"Bool\", ctx, \"ff-se-2887\", false).Return(true, nil).Once()\n\n\tcommunicationsSvc := NewService(emailSvc, smsSvc, memberServerClient, throttler, \"noreply@eleanorhealth.com\", ff)\n\n\terr := communicationsSvc.MemberTaskNotification(ctx, member, memberAppTasksURL)\n\tassert.NoError(err)\n}\n\nfunc TestService_MemberTaskNotification_smsDisabledByFlag_noEmailOnFile_noSend(t *testing.T) {\n\tassert := assert.New(t)\n\n\tmember := &domain.Member{\n\t\tID: entityid.DefaultGenerator.Generate(),\n\t\tNameFirst: \"John\",\n\t\tPhoneMobile: \"123-123-1234\",\n\t\tConsentToText: true,\n\t}\n\n\tctx := context.Background()\n\tmemberAppTasksURL := \"eleanor.health.com/tasks\"\n\n\temailSvc := email.NewMockService(t)\n\tsmsSvc := sms.NewMockService(t)\n\tmemberServerClient := memberserver.NewMockClient(t)\n\tthrottler := throttle.NewMockThrottler(t)\n\n\tdateEmailVerified := time.Now()\n\tmemberServerClient.On(\"Members\", ctx, &mserver.MembersOptions{\n\t\tHubID: member.ID,\n\t}).Return([]*mserver.Member{\n\t\t{DateEmailVerified: &dateEmailVerified},\n\t}, nil).Once()\n\n\tff := featureflag.NewMockService(t)\n\tff.On(\"Bool\", ctx, \"ff-se-2887\", false).Return(true, nil).Once()\n\n\t// No email or SMS call expected: SMS is flag-disabled, and member has no email on file.\n\tcommunicationsSvc := NewService(emailSvc, smsSvc, memberServerClient, throttler, \"noreply@eleanorhealth.com\", ff)\n\n\terr := communicationsSvc.MemberTaskNotification(ctx, member, memberAppTasksURL)\n\tassert.NoError(err)\n}\nGOEOF"]

input: ["mkdir -p /usr/local/bin","cat > /usr/local/bin/gpg-passphrase-wrapper <<'WRAPPER'\n#!/usr/bin/env bash\n_gpg_passphrase=\"${GPG_PASSPHRASE:-$(cat /run/secrets/gpg_passphrase 2>/dev/null)}\"\nif [[-z \"${_gpg_passphrase}\" && -f /lga/secrets/gpg_passphrase]]; then\n \_gpg_passphrase=\"$(cat /lga/secrets/gpg_passphrase)\"\nfi\nexec gpg --batch --passphrase \"${\_gpg_passphrase}\" --pinentry-mode loopback \"$@\"\nWRAPPER","chmod +x /usr/local/bin/gpg-passphrase-wrapper","cat /usr/local/bin/gpg-passphrase-wrapper"]

expected heredoc to not be split, got a split and invalid heredoc
